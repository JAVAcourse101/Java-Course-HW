import java.util.Scanner;

public class eight {

	public static void main(String[] args) {

		// Scanner sc = new Scanner(System.in);
		// System.out
		// .println("Please enter an array of whole numbers, but for doing that first tell me how long do you want it to be?");
		// int l = sc.nextInt();
		//
		// if (l <= 0) {
		// System.out
		// .println("Invalid input, please enter a whole positive number!!!");
		// sc.close();
		// return;
		// }
		//
		// int[] arr = new int[l];
		// System.out.println("Now let us start entering the array.");
		//
		// for (int u = 0; u < l; u++) {
		// arr[u] = sc.nextInt();
		//
		// }

		int[] arr = new int[] { 1, 1, 1, 12, 2, 2,2,2,2,2,2,2, 24, 4, 4, 4, 4,4, 5, 5, 5 };

		int length = 0;
		int lengthT = 0;
		int num = 0;

		for (int u = 0; u < arr.length - 1; u++) {
			if (arr[u] == arr[u + 1]) {
				lengthT++;
			}
			if ((arr[u] != arr[u + 1])&&(lengthT>length)) {
				length=lengthT;
				num=arr[u];
				lengthT=0;

			}

		}

		for (int i = 0; i <=length; i++) {
			System.out.print(num + " ");

		}

	}

}
